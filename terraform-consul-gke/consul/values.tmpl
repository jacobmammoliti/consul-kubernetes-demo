global:
    datacenter: ${datacenter}
    image: ${image_tag}
    acls:
        manageSystemACLs: ${enable_bootstrap_acls}

server:
    replicas: ${replicas}
    bootstrapExpect: ${replicas}

ui:
    service:
        type: ${service_type}
    enabled: ${ui_enabled}

connectInject:
    enabled: ${connect_injected_enabled}
    default: ${connect_injected_enabled_default}